-- ============================================================
--  ล้างข้อมูลทดสอบก่อนใช้งานจริง
--  ลบ: ออเดอร์/ยอดขาย/รับชำระ/เคลม/จ่ายสต๊อก/สต๊อกบนรถ/คิวส่ง/ค่าคอม/ค่าใช้จ่าย/ประวัติรับไข่เข้าคลัง
--  เก็บไว้: สินค้า, ลูกค้า, ราคาแยกราย, ผู้ใช้/บทบาท, ตั้งค่าร้าน
--  ** ทำก่อนเริ่มใช้จริง เท่านั้น — ลบแล้วกู้ไม่ได้ **
--  วิธีใช้: Supabase → SQL Editor → New query → วางทั้งไฟล์ → Run
-- ============================================================

truncate table
  order_items,
  payments,
  orders,
  claim_items,
  claims,
  allocation_items,
  allocations,
  stock_intake_items,
  stock_intakes,
  driver_stock,
  schedule,
  commissions,
  expenses
  restart identity cascade;

-- ตั้งสต๊อกคลังหลักเป็น 0 เพื่อเริ่มนับใหม่
-- (หลังรันเสร็จ ให้ไปที่เมนู "รับไข่เข้าคลัง" บันทึกไข่ที่มีอยู่จริงเข้าไป
--  หรือแก้ตัวเลขเองที่เมนู "สินค้า & สต๊อก")
update products set stock = 0;
