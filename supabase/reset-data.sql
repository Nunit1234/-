-- ============================================================
--  ล้างข้อมูลทดสอบก่อนใช้งานจริง
--  ลบ: ออเดอร์/ยอดขาย/รับชำระ/เคลม/จ่ายสต๊อก/สต๊อกบนรถ/คิวส่ง/ค่าคอม/ค่าใช้จ่าย
--  เก็บไว้: สินค้า, ลูกค้า, ราคาแยกราย, ผู้ใช้/บทบาท, ตั้งค่าร้าน
--  ** ทำก่อนเริ่มใช้จริง เท่านั้น — ลบแล้วกู้ไม่ได้ **
-- ============================================================

truncate table
  order_items,
  payments,
  orders,
  claim_items,
  claims,
  allocation_items,
  allocations,
  driver_stock,
  schedule,
  commissions,
  expenses
  restart identity cascade;

-- (ไม่บังคับ) รีเซ็ตสต๊อกคลังหลักเป็น 0 เพื่อเริ่มนับสต๊อกใหม่
-- เอา -- ออกหน้าบรรทัดล่างถ้าต้องการ:
-- update products set stock = 0;
